export const metadata = {
  title: "Distributed Order System",
  type: "Architecture Study"
};

# Distributed Order System

A project note for order lifecycle UI, domain event views, permission-aware actions, and operational dashboard states.
