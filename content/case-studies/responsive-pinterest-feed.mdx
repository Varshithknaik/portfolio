export const metadata = {
  title: "Responsive Masonry Feed",
  category: "Frontend Systems",
  readingTime: "12 min read"
};

# Responsive Masonry Feed

A masonry layout arranges variable-height items into columns with no fixed boundaries. 
These items of varying heights are packed into columns to eliminate vertical gaps,
resembling stacked bricks. This layout is commonly used for image grids, 
such as those on Pinterest or photo gallery websites.

## Problem Statement

Build an API-backed masonry image feed that adapts to container width, fills
tall viewports automatically, handles slow or failed images gracefully, and
keeps scroll performance stable as the feed grows.

## Design goal

Build a responsive Pinterest-style feed with these guarantees:

- pins are placed in the shortest column using the backend-provided frame height;
- the first viewport fills automatically without waiting for a scroll event;
- only one page request is active at a time;
- skeletons reserve realistic space until content is ready;
- pins are revealed in feed order — each image decodes and waits its turn;
- a failed or slow image settles with a fallback so the reveal queue advances;
- resize always recalculates from source data, never stale coordinates;
- loading, retry, completion, and accessibility states are explicit.

#### Future work

- virtualization to keep a bounded number of DOM nodes at scale.

### Non-goals

- recommendation/ranking logic;
- image upload or server-side image processing;
- live insertion of newly published pins;
- infinite scrolling inside the compact `card` preview.

## Architecture

- Page shell owns route and navigation boundaries.
- Feed client owns pagination, measurement, loading, and rendered pins.
- `/api/pins` returns paginated pin records.
- The masonry engine turns raw pins into `left`, `top`, `width`, and `height`.

## Diagram Slot

Add diagrams as regular markdown images:

```md
![Masonry architecture](/case-studies/Masonry/masonry-architecture.png)
```

Remote images are also supported when the URL uses `https`.

## Data Contract

```ts
type Pin = {
  id: number;
  url: string;
  alt: string;
  height: number;
};
```

## Layout math

```text
columnCount = max(1, floor((containerWidth + gap) / (minColumnWidth + gap)))
columnWidth = (containerWidth - (columnCount - 1) * gap) / columnCount

displayHeight = item.height
columnIndex = index of minimum(columnHeights)
left = columnIndex * (columnWidth + gap)
top = columnHeights[columnIndex]
bottom = top + displayHeight
columnHeights[columnIndex] = bottom + gap

totalHeight = max(columnHeights) - gap
```


## Performance Notes

- Use `ResizeObserver` to measure the feed container.
- Use `IntersectionObserver` for sentinel pagination.
- Decode images off-thread via `img.decode()` and reveal in feed order.
- Keep mutable pagination and image-loaded flags in refs.
- Add virtualization when DOM size becomes expensive.

## Accessibility Notes

- Every image needs useful `alt` text from the API or content pipeline.
- Keyboard users should be able to tab through pins in feed order.
- Loading state should not repeatedly announce noisy updates.
- Pin detail navigation should preserve scroll position when users return to the feed.

## Tradeoffs

Absolute positioning gives precise layout control but requires manual container height management. CSS columns are simpler but make ordering, measurement, and pagination harder to reason about.
